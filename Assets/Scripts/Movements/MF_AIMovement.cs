using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MF_AIMovement : MF_PMovement
{
    public override void move_pas(Vector2 moveValue)
    {
        //TODO Add AI moving
        //rgBody.AddForce(new Vector3(Input.GetAxis("Horizontal") * moveSpeed, 0, Input.GetAxis("Vertical") * moveSpeed));
    }
}
